// reducers.js
const initialState = {
  // Define el estado inicial aquí
};

const rootReducer = (state = initialState, action) => {
  switch (action.type) {
    // Define cómo el estado debe cambiar para cada acción
    default:
      return state;
  }
};

export default rootReducer;
